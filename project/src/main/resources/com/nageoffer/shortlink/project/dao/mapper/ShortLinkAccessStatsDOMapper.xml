<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nageoffer.shortlink.project.dao.mapper.ShortLinkAccessStatsDOMapper">


    <insert id="shortLinkStats">
        INSERT INTO t_link_access_stats
            (id,
             full_short_url,
            gid,
            `date`,
            pv,
            uv,
            uip,
            `hour`,
            weekday,
            create_time,
            update_time,
            del_flag
        ) VALUES (
        #{id},
        #{fullShortUrl},
        #{gid},
        #{date},
        #{pv},
        #{uv},
        #{uip},
        #{hour},
        #{weekday},
        NOW(),
        NOW(),
        0
        )
        ON DUPLICATE KEY UPDATE
                             pv = pv + #{pv},
                             uv = uv + #{uv},
                             uip = uip + #{uip},
                             update_time = NOW()
    </insert>
</mapper>